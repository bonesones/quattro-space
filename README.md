# Quattro Space Frontend

## Скрипты

- `npm install` — установка зависимостей
- `npm run dev` — запуск dev-сервера Vite
- `npm run build` — сборка production-билда в папку `dist`
- `npm run preview` — предпросмотр собранного билда
